<% if @item.destroyed? %>
  $('-#item-' +<%= @item.id %>).hide("fast")
<% else %>
   $('#item-' +<%= @item.id %>).prepend("<div class='alert alert-danger'><%= flash[:error] %></div> <br />");
<% end %>